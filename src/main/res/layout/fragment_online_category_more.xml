<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#fff"
     >

    <FrameLayout
        android:id="@+id/fragment_online_category_more_fmStateBar"
        android:layout_width="match_parent" android:layout_height="24dp"
    android:background="#000"></FrameLayout>

    <RelativeLayout
        android:id="@+id/fragment_online_category_more_re"
        android:layout_below="@id/fragment_online_category_more_fmStateBar"
        style="@style/titlebar" >


        <ImageButton
            android:id="@+id/fragment_online_category_more_return"
            android:layout_width="58dp"
            android:layout_height="match_parent"
            android:layout_alignParentLeft="true"
            android:background="@android:color/transparent"
            android:padding="17dp"
            android:scaleType="fitCenter"
            android:src="@drawable/back"
             />

        <TextView
            android:id="@+id/fragment_online_category_more_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:text="@string/localmusic"
            android:textSize="19sp" />
    </RelativeLayout>

    <!-- 具体歌曲列表 -->
    <ListView 
        android:id="@+id/fragment_online_category_more_listview"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:headerDividersEnabled="false"

        android:layout_below="@id/fragment_online_category_more_re"
        />

    <!-- titlebar阴影 -->
    <ImageView
        android:layout_width="match_parent"
        android:layout_height="3dp"
        android:layout_below="@id/fragment_online_category_more_re"
        android:scaleType="fitXY"
        android:src="@drawable/shadow"
         />
    
</RelativeLayout>
